plugins {
    id 'mapkit-samples.app'
}

android {
    namespace 'world.mappable.mapkitdemo.placemark'
    buildFeatures {
        buildConfig true
    }
}

dependencies {
    // Add MapKit SDK lite dependency
    implementation(name:'maps.mobile', ext:'aar')
}
